# Brew Desk Coffee

A simple Python coffee ordering app built with Tkinter. It lets you choose drinks, size, milk, quantity, pickup time, view totals, and save a text receipt.

## Run

```powershell
python coffee_app.py
```

Receipts are saved in the `receipts` folder after you click **Save receipt**.
